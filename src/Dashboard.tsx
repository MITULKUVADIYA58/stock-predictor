import React from 'react';

// Other imports...

const Dashboard = () => {
    // ...other code

    const navItems = [
        {
            title: 'Home',
            path: '/home',
        },
        {
            title: 'Analytics',
            path: '/analytics',
        },
        // Removed the Portfolio Navigation item
    ];

    return (
        <div>
            {/* Other component content */}
        </div>
    );
};

export default Dashboard;